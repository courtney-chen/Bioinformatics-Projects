#!/usr/bin/env bash
# check_command_execute.sh

# Specify the input path as an absolute path.
fastqPath="/scratch/AiptasiaMiSeq/fastq/"

# Print (echo) the variable
# echo ls -lh $fastqPath
# remove echo so the actual variable will be printed
ls -lh $fastqPath
